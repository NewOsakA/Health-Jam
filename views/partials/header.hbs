<header class="header">
  <nav class="container header__inner">
    <a href="/" class="brand brand-lg">🩺 Health Jam</a>
    <button
      class="nav-toggle"
      type="button"
      aria-expanded="false"
      aria-controls="siteNav"
    >
      <span class="sr-only">Toggle navigation</span>
      <span></span>
      <span></span>
      <span></span>
    </button>
    <div class="nav-shell" id="siteNav" data-nav>
      <ul class="nav-primary">
        <li><a href="/" class="{{active '/' path}}">Home</a></li>

        {{#if session.loggedIn}}
          <li class="dropdown nav-manage">
            <a
              href="/patients"
              class="{{activeMatch
                  '^/(patients|doctors|appointments|accounts)($|/)'
                  path
                }}"
            >
              Manage
            </a>
            <ul class="menu">
              <li><a href="/patients" class="{{active '/patients' path}}">•
                  Patients</a></li>
              <li><a href="/doctors" class="{{active '/doctors' path}}">•
                  Doctors</a></li>
              <li><a
                  href="/appointments"
                  class="{{active '/appointments' path}}"
                >• Appointments</a></li>

              {{#if session.is_admin}}
                {{! <li><hr
                  style="border:none;border-top:1px solid var(--line);margin:.35rem 0;"
                /></li> }}
                <li><a href="/accounts" class="{{active '/accounts' path}}">•
                    Accounts</a></li>
              {{/if}}
            </ul>
          </li>
        {{/if}}

        <li><a href="/about" class="{{active '/about' path}}">About</a></li>
        <li><a
            href="/contact"
            class="{{active '/contact' path}}"
          >Contact</a></li>
      </ul>
      <div class="nav-divider" aria-hidden="true">|</div>
      <div class="nav-auth">
        {{#if session.loggedIn}}
          <span class="hi">Hi, {{session.username}}</span>
          <a href="/logout" class="secondary">Logout</a>
        {{else}}
          <a href="/login" class="secondary {{active '/login' path}}">Login</a>
        {{/if}}
      </div>
    </div>
  </nav>
</header>