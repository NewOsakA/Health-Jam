<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{{#if title}}{{title}} · {{/if}}Health Jam Clinic</title>

    {{!-- Pico.css (CDN) --}}
    <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@2.0.6/css/pico.min.css" />

    {{!-- Google Font (optional) --}}
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header class="header">
      <nav class="container">
        <ul>
          <li><strong class="brand">🩺 Health Jam Clinic</strong></li>
        </ul>
        {{!-- Start: Code generated by ChatGPT --}}
        <ul class="nav-right">
          <li><a href="/" role="button" class="{{#if (eq title 'Home')}}contrast{{/if}}">Home</a></li>
          <li><a href="/patients" class="{{#if (eq title 'Patients')}}contrast{{/if}}">Patients</a></li>
          <li><a href="/doctors" class="{{#if (eq title 'Doctors')}}contrast{{/if}}">Doctors</a></li>
          <li><a href="/appointments" class="{{#if (eq title 'Appointments')}}contrast{{/if}}">Appointments</a></li>
          <li><a href="/about" class="{{#if (eq title 'About')}}contrast{{/if}}">About</a></li>
          <li><a href="/contact" class="{{#if (eq title 'Contact')}}contrast{{/if}}">Contact</a></li>
          {{#if session.loggedIn}}
            <li class="divider"></li>
            <li><span class="hi">Hi, {{session.username}}</span></li>
            <li><a href="/logout" class="secondary">Logout</a></li>
          {{else}}
            <li class="divider"></li>
            <li><a href="/login" class="{{#if (eq title 'Login')}}contrast{{/if}}">Login</a></li>
          {{/if}}
        </ul>
        {{!-- End: Code generated by ChatGPT --}}
      </nav>
    </header>

    <main class="container">
      {{{body}}}
    </main>

    <footer class="container footer">
      <small>© {{year}} Health Jam Clinic · Built with Node, Express, Handlebars, SQLite</small>
    </footer>
  </body>
</html>
